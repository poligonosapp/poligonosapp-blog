<%= form_with(model: polygons_post) do |form| %>
  <% if polygons_post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(polygons_post.errors.count, "error") %> prohibited this polygons_post from being saved:</h2>

      <ul>
        <% polygons_post.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
